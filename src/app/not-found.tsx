import { NotFoundState } from '@/components/common/not-found-state/not-found-state';

// Next.js konvansiyonu — bu dosya adı ve konumu (app/not-found.tsx) sabit.
export default function NotFound() {
  return <NotFoundState />;
}
